# Validation record — initial delivery, 2026-09-23

## Executed locally
**116 unit/integration tests passed; zero failures, errors or skips.** Environment: Python 3.13.5 on Linux. The raw test listing is `tests/last-run.txt`. `VALIDATION.json` records the machine-readable scope. Tests are independently named checks, including a complete synthetic run for each of the seven workflow routes.

Package invariants passed: twenty role contracts, seven acyclic workflows, nineteen source anchors, ten mapped source chapters, required references/files, scalar skill metadata, consistent package version and read-only CI configuration. Python compilation also passed. A separate JSON Schema check using the authoring environment’s `jsonschema` library validated all three schemas, seven request fixtures and 111 replayed agent artifacts; this library is not required by the package runtime.

An additional command-line multi-asset smoke run completed all twenty selected roles and ended **DEMO_COMPLETE_NOT_APPROVED**. It exercised persistent planning, evidence-to-analysis dependencies, separate instrument branches, review-packet generation and the explicit no-execution state. The replay is synthetic and made no LLM/API calls.

## Checked failure cases
Checks include missing/negative/non-finite inputs, missing Scope 3, inconsistent corporate intensity scope/year/currency, no sovereign mixing, incomplete WACI coverage, duplicate securities, invalid scenario probabilities, cash-flow/discount-rate double counting, terminal value constraints, unsupported callable pricing, bond chronology, rate/spread separation, evidence review and freshness, primary-policy-source requirements, future dates, cross-run artifacts, stale revisions, dependency cycles, frozen input changes, descendant/review invalidation, material exceptions, demo/source-study approval rejection and assumptions-only approval rejection.

The private publisher's local path/manifest checks, no-overwrite rule, authorization/network-versus-404 distinction and credential-absence behavior were unit tested without any external calls. Symlink, directory traversal, source-PDF and private-file exclusions are tested. Remote creation/push/verification has not been executed in this environment.

## Not performed
No remote repository creation or remote GitHub Actions run. No live issuer/market/climate feed integration. No model-generated research-output benchmark. No scientific probability, credit-risk, return-prediction or investment-performance calibration. No current legal, label or taxonomy certification. No exhaustive reading/audit of every paragraph of the 897-page source. No human investment approval of synthetic examples.

The included GitHub workflow is configured to test Python 3.10 and 3.12 after publication; those matrix jobs were not run in the authoring environment. Local success on Python 3.13.5 is not a claim that remote CI has passed.

## Interpretation
The result demonstrates that the stated software controls and synthetic calculations passed the included tests. It does not demonstrate that real source assertions are true, that an AI interprets them correctly, that the supplied assumptions are defensible, or that an investment should be made. Human review and appropriate external data/model integration remain necessary.
